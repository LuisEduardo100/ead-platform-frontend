import { EpisodeType } from '../../../services/courseService'
import styles from './styles.module.scss'

type epWithQuizzType = {
    episode: EpisodeType
}  
export default function QuizzButton({episode}: epWithQuizzType) {
    return <>
   
    
    </>
}